const config = {
  tabWidth: 2,
  printWidth: 100,
  endOfLine: 'auto',
  arrowParens: 'always',
  trailingComma: 'all',
  semi: true,
  useTabs: false,
  singleQuote: true,
  bracketSpacing: true,
  plugins: ['prettier-plugin-tailwindcss'],
};

export default config;
